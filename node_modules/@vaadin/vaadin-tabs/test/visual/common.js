import '../../vaadin-tab.js';
import '../../vaadin-tabs.js';
const $_documentContainer = document.createElement('template');

$_documentContainer.innerHTML = `<dom-module id="tab-styles" theme-for="vaadin-tab">
  <template>
    <style>
      :host,
      :host([selected]),
      :host::before,
      :host::after,
      :host::after {
        transition: none !important;
      }
    </style>
  </template>
</dom-module><dom-module id="tabs-styles" theme-for="vaadin-tabs">
  <template>
    <style>
      [part="forward-button"],
      [part="back-button"] {
        transition: none !important;
      }
    </style>
  </template>
</dom-module>`;

document.head.appendChild($_documentContainer.content);
